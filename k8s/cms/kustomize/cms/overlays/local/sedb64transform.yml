#
# Copyright SecureKey Technologies Inc. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

apiVersion: svceng
kind: SedB64Transformer
metadata:
  name: sedb64transformer
argsOneLiner: s^||DATABASE_USERNAME||^${DATABASE_USERNAME}^ s^||DATABASE_PASSWORD||^${DATABASE_PASSWORD}^ s^||DATABASE_CLIENT||^${DATABASE_CLIENT}^ s^||DATABASE_HOST||^${DATABASE_HOST}^ s^||DATABASE_PORT||^${DATABASE_PORT}^ s^||DATABASE_NAME||^${DATABASE_NAME}^
